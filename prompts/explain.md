Explain the code in the attached file. Work top-down:

1. FILE PURPOSE — one paragraph: what this file does and its place in the
   project (check AGENTS.md and docs/ for architecture context if present).
2. KEY SYMBOLS — for the main classes/functions: what each does, inputs,
   outputs, and any side effects. Note the symbol under the cursor
   ({{symbol}}) explicitly and explain it in detail, including where it is
   called from (`rg` for usages).
3. DATA FLOW — how data moves through this file (inputs, transforms,
   boundaries like I/O, serialization, domain logic).
4. RISKS / GOTCHAS — anything a reader must know: ordering assumptions,
   staleness or safety handling, error paths, idempotency concerns.
5. TRY THIS — 2-3 concrete next steps to explore (functions to read next,
   tests to look at, commands to run).

Format the reply as markdown with clear sections. Be concise; prefer bullets.
Reference file:line for anything important. Do not restate the code verbatim.
