---
description: Show the vim-ide cheatsheet (vim basics, IDE keybindings, agent workflows)
---

Read the cheatsheet file at {{CHEATSHEET_PATH}} and present it to the user in
the chat. Do not edit it.

Structure the reply as:
1. A short intro line (what vim-ide is).
2. The essential keybindings as a compact markdown table (vim survival keys,
   agent keys, diff keys, python toolchain keys).
3. The three-step daily loop (read / ask / review) in three bullets.
4. A closing line: the full guide is at {{CHEATSHEET_PATH}}; the user can ask
   you to explain any binding in detail.

Keep the reply faithful to the file — do not invent bindings. If the file
cannot be read, say so and ask the user for its location.
